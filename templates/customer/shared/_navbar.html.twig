<nav class="navbar navbar-expand-lg navbar-light bg-primary navbarcus">
    <div class="container-fluid">
        <a class="navbar-brand name" href="#">Chaussures</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">

            {% if app.user %}

                {% if app.user.roles[0] is same as "ROLE_ADMIN" %}
            
                    <li class="nav-item">
                        <a class="nav-link " aria-current="page" href="#">Administration</a>
                    </li>

                {% endif %}

                <li class="nav-item">
                    <a class="nav-link " aria-current="page" href="#">Se connecter</a>

            {% else %}
                
                <li class="nav-item">
                    <a class="nav-link " aria-current="page" href="#">Se connecter</a>

                </li>
                <li class="nav-item">
                    <a class="nav-link " aria-current="page" href="#">S'enregistrer</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link " aria-current="page" href="#">Catégories</a>
                </li>

            {% endif %}
                
            </ul>
        </div>
    </div>
</nav>